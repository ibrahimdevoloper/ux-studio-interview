# UX Studio Interview

## Instructions

Please contact me for delievring values to connect to the firebase at my email.

The values in the repo are disabled.

After receiving the secrect values,
Please, add them to the directory:
`UX studio interview\backend\src\main\resources\firebase\` under the file name `service-account.json`.

Please run the command
`docker-compose up`
to the front-end and back-end.

Wait a bit for the backend to start (2 to 5 mins).

Then open the browser to open `http://localhost:3000/`.

## Backend 
H2 SQL database, Firebase storage as storage bucket, Kotlin/spring boot to program the server.

## Frontend
Next.js for design development.







